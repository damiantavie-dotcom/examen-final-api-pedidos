{
  "name": "examen-final-api-pedidos",
  "version": "2.0.0",
  "description": "API REST para gestionar clientes y pedidos con SQLite relacional, Express, CRUD, validaciones y pruebas.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "init-db": "node scripts/init-db.js",
    "reset-db": "node scripts/reset-db.js",
    "test-api": "node scripts/test-api.js",
    "check": "node --check src/server.js && node --check src/app.js && node --check src/config/database.js && node --check src/controllers/clientes.controller.js && node --check src/controllers/pedidos.controller.js && node --check scripts/init-db.js && node --check scripts/test-api.js"
  },
  "keywords": [
    "nodejs",
    "express",
    "sqlite",
    "api-rest",
    "crud",
    "backend"
  ],
  "author": "Damian Cespedes",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "sql.js": "^1.13.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
